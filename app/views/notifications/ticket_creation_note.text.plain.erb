<%=_ 'Ticket #{{id}}: \'{{summary}}\'', :id => @ticket.id, :summary => truncate(@ticket.summary, :length => 50) %>
  <%= @ticket.previewable.link %>

-----------------------------------------------------------------------

  <%=_ "Reported by {{author}} - {{time}}", :author => @ticket.author, :time => @ticket.created_at.strftime(RetroCM[:content][:format][:datetime]) %>
<%= format_content(@ticket) -%>

-----------------------------------------------------------------------

<%=_ "You are receiving this message because you are subscribed to receive notifications about this ticket" %>
